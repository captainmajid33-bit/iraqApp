name: Build Android APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Install Dependencies
        run: npm install
      - name: Sync Capacitor
        run: npx cap sync android
      - name: Force Environment Compatibility
        run: |
          cd android
          # تحديث الـ Android Gradle Plugin إجبارياً إلى 8.9.1
          sed -i 's/8.3.2/8.9.1/g' build.gradle
          # التأكد من تحديث الـ Gradle Wrapper
          rm -rf gradle/wrapper/gradle-wrapper.properties
          cat <<EOF > gradle/wrapper/gradle-wrapper.properties
          distributionBase=GRADLE_USER_HOME
          distributionPath=wrapper/dists
          distributionUrl=https\://services.gradle.org/distributions/gradle-8.11.1-all.zip
          zipStoreBase=GRADLE_USER_HOME
          zipStorePath=wrapper/dists
          EOF
      - name: Build with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --warning-mode all
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
